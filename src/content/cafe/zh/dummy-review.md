---
title: "XR产品评测"
pubDate: 2026-01-06
description: "对XR产品的评测。"
tags: ["XR", "产品评测"]
status: "Seed"
locale: "zh"
draft: true
---

本文件位置在 `src/content/xr/zh/dummy-review.md`。

因为 `draft: true` 被设置:
- **Dev Mode (`npm run dev`)**: 这篇文章 **会** 出现在 XR 列表中。你可以点击它并查看浏览器中的样式。
- **Production (`npm run build`)**: 这篇文章将被 **排除**。它不会生成页面或出现在列表中。

当准备好发布时，只需要将 `draft: true` 改为 `draft: false`。
