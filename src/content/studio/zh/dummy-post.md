---
title: "AI 项目展示"
pubDate: 2026-01-09
description: "我的近期 AI 项目展示。"
tags: ["AI", "项目"]
status: "Sapling"
locale: "zh"
draft: true
---

本文件位置在 `src/content/studio/zh/dummy-post.md`。

因为 `draft: true` 被设置:
- **Dev Mode (`npm run dev`)**: 这篇文章 **会** 出现在 AI 列表中。你可以点击它并查看浏览器中的样式。
- **Production (`npm run build`)**: 这篇文章将被 **排除**。它不会生成页面或出现在列表中。

当准备好发布时，只需要将 `draft: true` 改为 `draft: false`。
